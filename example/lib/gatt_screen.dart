import 'package:flutter/material.dart';

class GattScreen extends StatelessWidget {
  const GattScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
